# Specification gap analysis

*Each requirement of `docs/handoff/NEURAXIS_EXECUTION_PLAN.pdf` (13 pages, received 2026-09-16), with
where it is met and what remains. Status: **met**, **partial**, **human** (needs the researcher),
**gated** (deliberately later).*

## Project identity and framing

| Requirement | Status | Where |
|---|---|---|
| Name Neuraxis, provisional until a documented name search | partial | `docs/NAME_AUDIT.md`: a trademark conflict was found; decision R-01 |
| Software title; positive and neutral manuscript titles; the positive title only if the held-out study shows silent drift | met | `CITATION.cff`; `manuscript/manuscript.md` states the rule |
| Not presented as a Claude Code benchmark; agent study secondary | met | `docs/PLAIN_LANGUAGE_OVERVIEW.md`; `docs/agent_study_protocol.md`; `workflows/run_agent_study.py` (gated) |
| Established components not claimed as original | met | `docs/PLAIN_LANGUAGE_OVERVIEW.md`, `PRIOR_ART_AUDIT.md`, `docs/PRIOR_ART_MATRIX.md` |
| Operational definitions (base model ... empirical behavioural certificate) | met | `docs/glossary.md` (earlier spec terms); plan terms mapped: `docs/FEATURE_CATALOG.md`, `docs/MUTATION_CATALOG.md`, `schemas.MutantClass` |
| Research questions and hypotheses; no target false-positive rate invented after held-out results | met (draft) | `docs/STATISTICAL_ANALYSIS_PLAN.md`, `docs/PREREGISTRATION_DRAFT.md` |

## Existing-repository continuation directive

| Requirement | Status | Where |
|---|---|---|
| Inventory branches, commits, tags, files, models, configs, results, manifests, audits, plans | met | `docs/CURRENT_REPOSITORY_STATE.md` |
| Classify work as complete, partial, unverified, obsolete or conflicting; locate NeuroSem/PerturbPrint/Neuraxis references | met | same, sections 1, 4, 5 |
| Overwritten raw data? Held-out outcomes accessed? Result status? | met | same, section 2: no overwrite (hash check 2026-09-16); no held-out split or access; Pilot 1 exploratory |
| Reuse verified components; repair invalid ones; new run IDs for reruns | met | X-05, X-06 in `docs/DEVIATION_LOG.md` |
| Record every conflict in `docs/DEVIATION_LOG.md`; never relabel inspected data as confirmation data | met | `docs/DEVIATION_LOG.md`; `experiments/registry.py` enforces roles |
| Leakage handling | met (none found) | `docs/CURRENT_REPOSITORY_STATE.md` section 2 |

## Required documents

| Document | Status |
|---|---|
| `docs/CURRENT_REPOSITORY_STATE.md`, `SPECIFICATION_GAP_ANALYSIS.md`, `PLAIN_LANGUAGE_OVERVIEW.md`, `DEVELOPMENT_PROTOCOL.md`, `FEATURE_CATALOG.md`, `DEVIATION_LOG.md`, `DECISIONS_REQUIRED.md` | met (new) |
| `docs/PRIOR_ART_MATRIX.md` (≥20 rows, five verification packets) | met; researcher inspection pending (human) |
| `docs/DEPENDENCY_AUDIT.md` | met (moved from the root; re-verified against PyPI and GitHub releases on 2026-09-16) |
| `docs/LICENSING_MATRIX.md` | met (generated by `scripts/build_manifests.py`) |
| `docs/NAME_AUDIT.md` | met; decision pending |
| `docs/PROTOCOL_CATALOG.md`, `MUTATION_CATALOG.md`, `STATISTICAL_ANALYSIS_PLAN.md`, `PREREGISTRATION_DRAFT.md`, `AI_DISCLOSURE.md` | met (renamed) |
| `AI_USE_LOG.md`, `CHANGELOG.md` | met (updated) |

## Repository architecture

| Plan item | Here | Status |
|---|---|---|
| `README.md`, `LICENSE`, `CITATION.cff`, `pyproject.toml`, `environment.yml`, `Dockerfile`, `Makefile`, `CHANGELOG.md`, `AI_USE_LOG.md` | present | met (Dockerfile never built: no Docker here) |
| `configs/development.yaml` | `configs/study.yaml` plus `configs/<development protocol>/` | equivalent |
| `configs/heldout_frozen.yaml` | `configs/FROZEN.lock` over all frozen configs (`workflows/freeze_study.py`) | gated |
| `configs/protocols.yaml` | `configs/study.yaml` `protocols` and `canonical` sections | equivalent |
| `configs/features.yaml`, `configs/tolerances.yaml` | present | met |
| `configs/mutations.yaml` | `configs/study.yaml` `pilot` section (families, exclusions, counts) plus operator grids in code | equivalent |
| `configs/selection.yaml`, `configs/statistics.yaml` | `configs/study.yaml` `selection` and `analysis` sections | equivalent |
| `configs/agent.yaml` | `configs/agent_policy.yaml` | equivalent |
| `manifests/{models,protocols,mutations,transformations,development_split,heldout_split}.csv`, `manifests/hashes/` | generated | met (`heldout_split.csv` header-only: not assigned) |
| `models/{raw,curated,fixtures}` | `models/raw` (in use), `models/candidates` (under curation); fixtures are the two smoke-test models in `models/raw` | equivalent |
| `neuraxis/{cli,configuration,provenance,simulators,protocols,features,mutations,transformations,validation,selection,orchestration,analysis}` | `src/neuraxis/` with `config.py`, `transforms/`, `experiments/` (campaign orchestration) and `orchestration/` (curation, smoke test) | equivalent |
| `tests/{unit,integration,regression,fixtures}` | `tests/unit`, `tests/integration`, `tests/regression`; fixtures come from `models/raw` through `conftest.py` | met |
| `workflows/` (12 named steps) | all 12 present: `curate_models`, `run_references`, `calibrate_tolerances`, `generate_variants`, `run_development`, `select_protocols`, `freeze_study`, `run_heldout`, `lock_primary_results`, `run_robustness`, `run_agent_study`, `reproduce_manuscript` (plus `smoke_test`) | met |
| `results/{raw,processed,audits,tables,figures}` | present (`audits/` added) | met |
| `manuscript/{manuscript,supplement,cover_letter}.md` | skeletons without results | met (no claims) |

## Environment, smoke test, provenance, curation, licensing

| Requirement | Status | Where |
|---|---|---|
| Verified versions of Python, Java, NeuroML/LEMS tooling, pyNeuroML, jNeuroML, eFEL, NumPy, SciPy, pandas, plotting, statistics, pytest | met | `docs/DEPENDENCY_AUDIT.md`, `requirements.lock` (all at the latest release on 2026-09-16 except ruff, one patch behind) |
| Type checking and linting | partial | ruff pinned; mypy not installed (X-15) |
| Clean-environment mechanism | partial | fresh virtual environment from `requirements.lock` (smoke test); Docker not available |
| Infrastructure smoke test (9 items); failure blocks scaled collection | met | `neuraxis smoke-test`; report in `results/audits/smoke_test/` |
| Structured run record with the plan's fields; raw never overwritten; skip only after hash verification | met | `schemas.RunRecord`, `RUN_RECORD_REQUIRED_FIELDS`, `validation/execution.py` |
| Model inclusion and exclusion criteria; exclusion independent of results; diversity | met (tool) / partial (pool size) | `neuraxis curate-models`; `docs/MODEL_CURATION_REPORT.md` |
| Licensing plan: Apache-2.0 provisional; per-model provenance record; no relicensing; downloader when redistribution is unclear | met / human | `LICENSE`, `docs/LICENSING_MATRIX.md`, `scripts/fetch_models.py`; human licence review pending (L-11) |

## Phases and gates

| Requirement | Status | Where |
|---|---|---|
| Autonomous infrastructure, curation, calibration, variant generation, development execution and analysis; then stop for human audit | infrastructure met; development execution waits for R-02 and R-03 | `docs/DEVELOPMENT_PROTOCOL.md` |
| Held-out only after freeze, registration and authorisation | gated | `workflows/freeze_study.py`, `workflows/run_heldout.py` (exact sentence, AsPredicted URL and PDF), `HeldoutGate` |
| Mutation-family holdout; kinetics excluded from protocol selection | met | `experiments/strata.py` `SELECTION_EXCLUDED_FAMILIES`; `experiments/discovery.py` |
| Lock primary results after the held-out run | met (tool) | `workflows/lock_primary_results.py` |
