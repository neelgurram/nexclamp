# Manifests

Generated by `scripts/build_manifests.py` (and `neuraxis curate-models` for `models.csv`). `heldout_split.csv` is header-only until Neel assigns the held-out split; `hashes/` holds per-file SHA-256 of every stored model snapshot. `pilot_pre_run_*` files belong to PILOT_PROTOCOL_V1.
